Add files from Julia to Git repo:
git init

git add .

git commit -m "Add all my files"

git remote add origin https://github.com/jiangyanyu/AI_BATS.git

git pull origin master

git push origin master
